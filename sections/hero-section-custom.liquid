{% schema  %} 
    
{
    "name": "Hero Section",
    "settings": [
        {
            "id": "hero_section",
            "type": "text",
            "label": "text",
            "default": "insert text here"

        }
        
    ],
    "blocks": [
        {
            "type": "image_picker",
            "name": "Image Block",
            "settings": [
                {
                    "id": "block_image",
                    "type": "image_picker",
                    "label": "text_image"

                }
            ]

                },
                {
                    "type": "text",
                    "name": "text Line",
                    "settings": [{
                        "id": "text_line",
                        "type": "text",
                        "label": "text_line"
                    }]
                }
    ]
}
 {% endschema %}

 <div>
<h1>{{settings.hero_section}}</h1>
{% for block in blocks %}
    {% if block.type =='text_line' %}
        <p>{{block.settings.text_line}}</p>
        {% endif %}
    {% if block.type == 'image_picker' %}
<img width=500px height="500px" src="{{block.settings.block_image}}" alt="hero image">
{% endif %}
{% endfor %}
 </div> 